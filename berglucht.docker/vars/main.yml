---
# vars file for docker
DOCKER_DEPENDS:
  - ca-certificates
  - curl
  - gnupg
  - lsb-release
  - python3-apt

DOCKER_PACKAGES:
  - docker-ce 
  - docker-ce-cli 
  - containerd.io 
  - docker-compose-plugin

DOCKER_HARDEN_PACKAGES:

